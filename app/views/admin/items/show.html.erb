 <div class="container">
  <div class="row">
    <div class="col-lg-12">
      <h3 class="title">商品詳細</h3>
    </div>
  </div>
  <div class="row">


    <div class="col-lg-4 text-center">
      <%= attachment_image_tag @item, :image, format: 'jpeg', size: "300x200", fallback: "no_image.jpg" %>
    </div>
    <br>

    <div class="col-lg-8">


      商品名　　　<%= @item.name %><br><br>


      商品説明　　<%= @item.introduction %><br><br>


      ジャンル　　<%= @item.genre.name %><br><br>


      税込価格（税抜価格）　　<%= @tax.to_i.to_s(:delimited) %>（<%= @item.price.to_s(:delimited) %>） 円<br><br>


      販売ステータス　　<%= @item.is_active ? "販売中" : "販売停止中" %><br><br>


      <div class="text-right">
        <%= link_to '編集する', edit_admin_item_path, class: "btn btn-primary" %>
      </div>
      <div class="text-right">
        <%= link_to '商品一覧', admin_items_path, class: "btn btn-primary" %>
      </div>
    </div>
  </div>
</div>

